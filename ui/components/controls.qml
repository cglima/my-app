import QtQuick
import QtQuick.Controls

Item{
    width: 200
    height: 100

    Button {
        text: "Iniciar a leitura"
        //onClicked: {
            // logica para inic leitura
        //}
        anchors.centerIn: parent
    }
}